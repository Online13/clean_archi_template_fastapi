[project]
name = "create-fastapi-app"
version = "0.1.9"
description = "A CLI tool to create a FastAPI project."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []


[project.scripts]

create-fastapi-app = "create_fastapi_app.__main__:main"